<!DOCTYPE html>
<html>
  <head>
    <title>Apple Forecast</title>
    <meta charset="utf-8" />
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <style>
      body { font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; margin: 2rem; background-color: #f5f5f7; }
      .container { max-width: 720px; margin: 0 auto; background: #fff; padding: 2rem; border-radius: 12px; box-shadow: 0 4px 20px rgba(0,0,0,0.05); }
      h1 { font-size: 2rem; margin-bottom: 1.5rem; }
      form label { display: block; margin-bottom: 0.5rem; font-weight: 600; }
      form input[type="text"] { width: 100%; padding: 0.75rem; border-radius: 8px; border: 1px solid #d2d2d7; font-size: 1rem; }
      form input[type="submit"] { margin-top: 1rem; padding: 0.75rem 1.5rem; border: none; border-radius: 8px; background: #0071e3; color: #fff; font-size: 1rem; cursor: pointer; }
      .flash { padding: 0.75rem 1rem; border-radius: 8px; margin-bottom: 1rem; }
      .flash.notice { background: #e0f2ff; color: #0b5394; }
      .flash.alert { background: #fdecea; color: #d93025; }
      .forecast-card { background: #f9f9fa; border-radius: 12px; padding: 1.5rem; margin-top: 1.5rem; }
      .forecast-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(160px, 1fr)); gap: 1rem; margin-top: 1rem; }
      .forecast-grid div { background: #fff; border-radius: 12px; padding: 1rem; box-shadow: inset 0 0 0 1px #e5e5ea; }
      .meta { font-size: 0.875rem; color: #6e6e73; margin-top: 0.5rem; }
    </style>
  </head>
  <body>
    <div class="container">
      <% flash.each do |type, message| %>
        <div class="flash <%= type %>"><%= message %></div>
      <% end %>
      <%= yield %>
    </div>
  </body>
</html>
